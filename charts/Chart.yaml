apiVersion: v2
name: kargo-creds-cron
description: Sync Kargo credential Secrets from a source namespace into an Akuity Kargo instance using the Akuity CLI.
type: application
version: 0.4.0
appVersion: "v0.4"
